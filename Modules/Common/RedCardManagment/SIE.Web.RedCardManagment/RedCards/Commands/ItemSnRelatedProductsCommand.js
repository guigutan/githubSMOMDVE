SIE.defineCommand('SIE.Web.RedCardManagment.RedCards.Commands.ItemSnRelatedProductsCommand', {    meta: { text: "关联产品", group: "edit", iconCls: "icon-FormatListBulletedType icon-blue" },    executeIntervalMode: SIE.cmd.IntervalMode.Debounce.value,//使用防抖模式    canExecute: function (view) {        return view.getParent().getCurrent();    },    execute: function (view) {        var redCard = view.getParent().getCurrent();        var redCardId = redCard.getId();        var snList = view.getSelection().map(function (data) { return data.getSN() });        var params = null;        if (redCard) {            params = {                RedCardId: redCardId,                SnList: snList            }        };        CRT.Workbench.addPage({            title: '产品追溯清单'.t(),            entityType: 'SIE.RedCardManagment.RedCards.ProductRetroactive',            viewGroup: "ListView",            module: view.getParent().module,            isAggt: true,            params: params        });    }}); 