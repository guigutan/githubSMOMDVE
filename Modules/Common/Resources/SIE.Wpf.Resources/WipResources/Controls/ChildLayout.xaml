<UserControl x:Class="SIE.Wpf.Resources.WipResources.Controls.ChildLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SIE.Wpf.Resources.WipResources.Controls"
             xmlns:uic="http://schemas.siestudio.com/winfx/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="-10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4.2*"/>
        </Grid.ColumnDefinitions>
        <dxg:GridControl Name="resCalendarControl" SelectionMode="Cell" Grid.Column="0">
            <dxg:GridControl.Columns>
                <dxg:GridColumn 
                    Width="90"
                    FieldName="Name"
                    Header="{Binding [名称], Source={uic:L10N}}"
                    ReadOnly="True"
                    ToolTip="{Binding Name}" />
                <dxg:GridColumn 
                    Width="90"
                    FieldName="ShiftType.Name"
                    Header="{Binding [班制], Source={uic:L10N}}"
                    ReadOnly="True"
                    ToolTip="{Binding Name}" />
                <dxg:GridColumn 
                    Width="120"
                    FieldName="ActiveDate"
                    Header="{Binding [预设启用日期], Source={uic:L10N}}"
                    ReadOnly="True">
                    <dxg:GridColumn.EditSettings>
                        <dxe:DateEditSettings
                            Mask="F"
                            MaskType="DateTime"
                            MaskUseAsDisplayFormat="True"
                            ShowWeekNumbers="True" />
                    </dxg:GridColumn.EditSettings>
                </dxg:GridColumn>
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView BestFitMode="AllRows" ShowGroupPanel="False" />
            </dxg:GridControl.View>
        </dxg:GridControl>
        <local:ResCalendarControl x:Name="calendar" Grid.Column="1" Margin="5,-5,5,-5"/>
    </Grid>
</UserControl>
