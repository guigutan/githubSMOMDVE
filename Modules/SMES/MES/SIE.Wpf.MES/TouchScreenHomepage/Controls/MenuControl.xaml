<UserControl
    x:Class="SIE.Wpf.MES.TouchScreenHomepage.MenuControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" x:Name="allScrollViewer">
        <Grid Margin="55,55,5,0"  HorizontalAlignment="Center" >
            <Grid.Resources>

                <SolidColorBrush x:Key="brush" Color="#C1C1C1" />
                <Style x:Key="TabButton" TargetType="Button">
                    <Setter Property="TextBlock.TextAlignment" Value="Center" />
                    <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
                    <Setter Property="Width" Value="430" />
                    <Setter Property="Height" Value="180" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border
                                    Margin="0,25,75,25"
                                    BorderBrush="#FFF3F3F3"
                                    BorderThickness="0.2"
                                    Opacity="0.8"
                                    CornerRadius="5">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="1,0" EndPoint="1,1"   Opacity="0.4">
                                            <GradientStop Color="Transparent" Offset="0"/>
                                            <GradientStop Color="#FFF3F3F3" Offset="1"/>

                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <ContentPresenter
                                        x:Name="contentPresenter"
                                        Margin="{TemplateBinding Padding}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        />

                                </Border>

                            </ControlTemplate>

                        </Setter.Value>

                    </Setter>
                </Style>
            </Grid.Resources>
            <Border
                Grid.Row="0"
                Grid.Column="0"
                BorderBrush="{StaticResource brush}"
                BorderThickness="0,0,0,0">

                <ItemsControl x:Name="ctlMenu" Margin="0,0,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border>
                                <Button
                                    x:Name="btnMenu"
                                    Click="btnMenu_Click"
                                    Cursor="Hand"
                                    Style="{StaticResource TabButton}"
                                    Tag="{Binding Id}">
                                    <TextBlock FontSize="35px" Foreground="White"  Text="{Binding Label}" />
                                   
                                </Button>
                            </Border>
                        </DataTemplate>
                        
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" />
                        </ItemsPanelTemplate>
                        
                    </ItemsControl.ItemsPanel>
                    
                </ItemsControl>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
