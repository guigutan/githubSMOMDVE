<UserControl     x:Class="SIE.Wpf.MES.Controls.ResponsibilityControl"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:uic="http://schemas.siestudio.com/winfx/xaml/core"
                 mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <DockPanel>
            <dxdo:LayoutGroup DockPanel.Dock="Bottom" Caption="{Binding [备注],Source={uic:L10N}}" ShowCaption="True" Margin="5,0,5,5" GroupBorderStyle="GroupBox" ItemWidth="200" AllowExpand="False">
                <dxdo:LayoutControlItem ShowCaption="False">
                    <TextBox Name="txtRemark" Text="{Binding Remark,Mode=TwoWay}" AcceptsReturn="True" MaxWidth="220" Height="100" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                    <!--<dxe:TextEdit Name="txtRemark" Text="{Binding Remark,Mode=TwoWay}" AcceptsReturn="True" MaxWidth="220" Height="100" TextWrapping="Wrap" NullText="{Binding [备注],Source={uic:L10N}}" VerticalScrollBarVisibility="Auto"/>--> 
                </dxdo:LayoutControlItem>
            </dxdo:LayoutGroup>
            <dxdo:LayoutGroup Caption="{Binding [已选缺陷责任],Source={uic:L10N}}" DockPanel.Dock="Left" ShowCaption="True" GroupBorderStyle="GroupBox" AllowExpand="False" ItemWidth="200" Orientation="Vertical" Margin="5" ItemHeight="auto">
                <dxdo:LayoutControlItem ShowCaption="False">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Width="220">
                        <ItemsControl Name="ctlSelect" MinHeight="40">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <dx:SimpleButton Margin="5" Tag="{Binding}" x:Name="btnSelected" Click="BtnSelected_Click">
                                        <Grid MinWidth="76" Height="40">
                                            <Label Content="{Binding Responsibility.Description}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <TextBlock VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0 -4 -1 0" Text="x"/>
                                            <TextBlock Visibility="Hidden" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0 0 -1 0" Text="{Binding Qty}"/>
                                        </Grid>
                                    </dx:SimpleButton>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel IsItemsHost="True"></WrapPanel>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </dxdo:LayoutControlItem>
            </dxdo:LayoutGroup>
        </DockPanel>

        <dxdo:LayoutGroup Caption="{Binding [缺陷责任分类],Source={uic:L10N}}" DockPanel.Dock="Top" ShowCaption="True" GroupBorderStyle="GroupBox" ItemHeight="Auto"  Margin="0,5">
            <dxdo:LayoutControlItem ShowCaption="False">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <dx:SimpleButton Margin="5 5 0 5" MinWidth="60" Height="30" x:Name="btnAll" Click="BtnAll_Click" Content="{Binding [所有],Source={uic:L10N}}" ToolTip="{Binding [所有],Source={uic:L10N}}" />
                            <ItemsControl Name="ctlNavigation">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <dx:SimpleButton Margin="2 5 0 5" MinWidth="60" Height="30" x:Name="btnNavigation" Click="BtnNavigation_Click" Content="{Binding Category.Description}" ToolTip="{Binding Category.Description}" Tag="{Binding}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel IsItemsHost="True"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </StackPanel>
                        <ItemsControl Name="ctlCategory">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <dx:SimpleButton  Margin="5" MinWidth="80" Height="40" x:Name="btnCategory" Click="BtnCategory_Click" Content="{Binding Category.Description}" ToolTip="{Binding Category.Description}" Tag="{Binding}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </dxdo:LayoutControlItem>
        </dxdo:LayoutGroup>

        <dxdo:LayoutGroup Caption="{Binding [缺陷责任代码],Source={uic:L10N}}" DockPanel.Dock="Top" ShowCaption="True" GroupBorderStyle="GroupBox" AllowExpand="False" ItemHeight="Auto">
            <dxdo:LayoutControlItem ShowCaption="False">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <ItemsControl Name="ctlDefect">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <dx:SimpleButton Margin="5" Width="80" Height="40" x:Name="btnDefect" Click="BtnDefect_Click" Content="{Binding Responsibility.Description}"  Tag="{Binding}" ToolTip="{Binding Responsibility.Description}"></dx:SimpleButton>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </dxdo:LayoutControlItem>
        </dxdo:LayoutGroup>
    </DockPanel>
</UserControl>
