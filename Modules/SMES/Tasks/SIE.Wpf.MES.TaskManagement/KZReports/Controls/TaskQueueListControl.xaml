<UserControl x:Class="SIE.Wpf.MES.TaskManagement.KZReports.Controls.TaskQueueListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SIE.Wpf.MES.TaskManagement.KZReports.Controls" 
             xmlns:conv="clr-namespace:SIE.Wpf.MES.TaskManagement.KZReports.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary >
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SIE.Wpf.MES.TaskManagement;component/Styles/ReportStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <conv:TaskStatusConverter x:Key="TaskStatusConverter"/>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Height" Value="50"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border Background="#E0E8EF" BorderThickness="0" BorderBrush="Black">
                                <TextBlock Text="{Binding}"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="Black"
                               FontFamily="Times New Roman"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="DataGridRow">
                <Setter Property="BorderThickness" Value="0.5"/>
                <Setter Property="BorderBrush" Value="#e0e8eF"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value=" true">
                        <Setter Property="Background" Value="#F0F8FF"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="DataGridCell">
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="BorderBrush" Value="#e0e8eF"/>
                <Setter Property="Height" Value="50"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value=" true">
                        <Setter Property="Background" Value="IndianRed"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Normal"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="LayoutTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1.5" ScaleY="1.5" />
                    </Setter.Value>
                </Setter>

            </Style>
        </ResourceDictionary>        
    </UserControl.Resources>
    <Border Background="White" MaxWidth="1400" VerticalAlignment="Stretch">

        <Grid Background="White" Margin="30">
            <Grid.RowDefinitions >
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0 0">
                <StackPanel  Grid.Column="0" Orientation="Horizontal"  Margin="0 0 0 0" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Label Content="生产队列清单:" FontSize="24"  HorizontalAlignment="Left" Height="40"/>
                    <!--<Button Name="btnTaskQueue" Content="生产任务队列" Style="{StaticResource CommonButtonStyle}" Width="120" Height="40" Margin="20 0" Click="btnTaskQueue_Click" Background="DeepSkyBlue" ></Button>-->
                    <Button Content="添加任务" Style="{StaticResource CommonButtonStyle}" x:Name="btnTaskList"  Click="btnTaskList_Click" Height="40" Margin="30 0 10 0" Background="DeepSkyBlue"></Button>
                    <Button Content="共模生产" Style="{StaticResource CommonButtonStyle}" x:Name="btnCommonMode"  Click="btnCommonMode_Click" Height="40" Margin="10 0" Background="DeepSkyBlue"></Button>
                </StackPanel>

                <StackPanel  Orientation="Horizontal" Height="40" Margin="0 0 0 0" HorizontalAlignment="Right">
                    <Label Content="报工资源" FontSize="18" FontWeight="Bold" Foreground="Gray" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Margin="5" ></Label>
                    <Label Content="{Binding Resource.Name}" Name="lbResourceName" Foreground="Blue" Margin="0 0 20 0" MinWidth="80" Width="auto" Height="40" FontSize="18" FontWeight="Bold" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" ></Label>

                    <Label Content="报工操作员:" FontSize="18" FontWeight="Bold" Foreground="Gray" VerticalContentAlignment="Center" HorizontalContentAlignment="Right" Margin="5" ></Label>
                    <Label Content="{Binding ReportEmployee.Name}" Name="lbOparetorName" Foreground="Blue" Margin="0 0 10 0" MinWidth="80" Width="auto" Height="40" FontSize="18" FontWeight="Bold" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" ></Label>
                </StackPanel >
            </Grid>
            <Grid Name="templeteGrid" Grid.Row="1" Margin="0 0" VerticalAlignment="Top">

                <Border Grid.Row="0" Grid.Column="0"  BorderBrush="Gray" BorderThickness="0,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Top">
                    <DataGrid x:Name="dataGridRecord" MinHeight="400" BorderThickness="0" AutoGenerateColumns="False" FontSize="18" BorderBrush="#EEEEEE" Margin="0"
				   HorizontalAlignment="Stretch"   VerticalAlignment="Stretch"  CanUserSortColumns="True" IsReadOnly="True" 
					 SelectionMode="Single" GridLinesVisibility="None"  >

                        <DataGrid.Columns>
                            <!--<DataGridCheckBoxColumn Header="选择" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"/>-->

                            <DataGridTemplateColumn Header="选中" Width="50" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <!-- 复选框启用编辑，绑定到IsSelected属性 -->
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                            </DataGridTemplateColumn>
                            <!-- 其他列 -->
                            <DataGridTextColumn Header="Id" Visibility="Hidden" Binding="{Binding Id}" />
                            <DataGridTemplateColumn Header="序号"  Width="60" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Seq}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Seq}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="计划完成时间"  Width="140" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding PlanEndTime, StringFormat=yyyy-MM-dd}" TextTrimming="CharacterEllipsis" ToolTip="{Binding PlanEndTime, StringFormat=yyyy-MM-dd HH:mm}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="工单号"  Width="200" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding WorkOrderNo}" TextTrimming="CharacterEllipsis" ToolTip="{Binding WorkOrderNo}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="派工单号"  Width="200" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DispatchTaskNo}" TextTrimming="CharacterEllipsis" ToolTip="{Binding DispatchTaskNo}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="产品编码"  Width="150" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ProductCode}" TextTrimming="CharacterEllipsis" ToolTip="{Binding ProductCode}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="产品名称" Width="300">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ProductName}" ToolTip="{Binding ProductName}" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap"  FontSize="16" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="旧料号" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ShortDescription}" ToolTip="{Binding ShortDescription}" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap"  FontSize="16" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="父级旧料号" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ParShortDescription}" ToolTip="{Binding ParShortDescription}" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap"  FontSize="16" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <!--<DataGridTemplateColumn Header="规格型号"   Width="150" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SpecificationModelName}" TextTrimming="CharacterEllipsis" ToolTip="{Binding SpecificationModelName}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>-->
                            <DataGridTemplateColumn Header="任务数量"  Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DispatchQty}" TextTrimming="CharacterEllipsis" ToolTip="{Binding DispatchQty}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="状态"  Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding TaskStatus, Converter={StaticResource TaskStatusConverter}}" TextTrimming="CharacterEllipsis" ToolTip="{Binding TaskStatus}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="工序名称"  MinWidth="150" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding ProcessName}" TextTrimming="CharacterEllipsis" ToolTip="{Binding ProcessName}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>

                        <DataGrid.Resources>
                            <!-- 应用自定义滚动条样式 -->
                            <!--<Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource HorizontalScrollBarStyle}"/>-->
                        </DataGrid.Resources>
                    </DataGrid>
                </Border>

            </Grid>
            <StackPanel Grid.Row="2" Margin="0 0 0 10" Background="Transparent"  Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="返回" Style="{StaticResource CommonButtonStyle}" Name="btnBack" Click="btnBack_Click" Width="110" Height="50" Margin="10 0"  Background="Gray" ></Button>
                <Button Content="暂停" Style="{StaticResource CommonButtonStyle}" Name="btnPause" Click="btnPause_Click" Width="110" Height="50" Margin="10 0" Background="DeepSkyBlue"></Button>
                <Button Content="执行" Style="{StaticResource CommonButtonStyle}" Name="btnExcute"  Click="btnExcute_Click" Width="110" Height="50" Margin="10 0" Background="DeepSkyBlue"></Button>
                <Button Content="上移" Style="{StaticResource CommonButtonStyle}" Name="btnUp" Click="btnUp_Click" Width="110" Height="50" Margin="10 0" Background="DeepSkyBlue"></Button>
                <Button Content="下移" Style="{StaticResource CommonButtonStyle}" Name="btnDown"  Click="btnDown_Click" Width="110" Height="50" Margin="10 0" Background="DeepSkyBlue"></Button>
                <Button Content="移除" Style="{StaticResource CommonButtonStyle}" Name="btnRemove"  Click="btnRemove_Click" Width="110" Height="50" Margin="10 0" Background="DeepSkyBlue"></Button>
                <Button Content="确定" Style="{StaticResource CommonButtonStyle}" Name="btnConfirm" Click="btnConfirm_Click" Width="110" Height="50" Margin="10 0" ></Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
