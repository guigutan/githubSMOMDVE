<UserControl x:Class="SIE.Wpf.MES.TaskManagement.ReportDefectControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:uic="http://schemas.siestudio.com/winfx/xaml/core"
             xmlns:converter="clr-namespace:SIE.Wpf.MES.TaskManagement"
             xmlns:local="clr-namespace:SIE.Wpf.MES.TaskManagement"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <DockPanel.Resources>
            <converter:QtyVisibilityConverter x:Key="QtyVisibilityConverter"/>
            <Style x:Key="ButtonNavStyle" TargetType="{x:Type Button}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Grid SnapsToDevicePixels="True">
                                <Path x:Name="border" Data="M179.5,119.5 L219.5,119.5 224.5,129.5 219.5,139.5 179.5,139.375 179.5,119" Stretch="Fill" Stroke="{DynamicResource ControlBorderBrush}" Fill="{DynamicResource Brush3}"/>
                                <ContentPresenter Margin="0 0 6 0" x:Name="contentPresenter" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True"/>
                                <Trigger Property="IsDefaulted" Value="True"/>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Fill" TargetName="border" Value="{DynamicResource Brush2}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Fill" TargetName="border" Value="{DynamicResource Brush1}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False"/>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border x:Name="border" BorderBrush="{DynamicResource ControlBorderBrush}" BorderThickness="1">
                                <Grid x:Name="grid" Background="{DynamicResource Brush3}">
                                    <ContentPresenter Margin="0 0 6 0" x:Name="contentPresenter" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True"/>
                                <Trigger Property="IsDefaulted" Value="True"/>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" TargetName="grid" Value="{DynamicResource Brush2}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" TargetName="grid" Value="{DynamicResource Brush1}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False"/>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </DockPanel.Resources>
        <dxdo:LayoutGroup Caption="{Binding [已选缺陷],Source={uic:L10N}}" DockPanel.Dock="Left" ShowCaption="True" GroupBorderStyle="GroupBox" AllowExpand="False" ItemWidth="230" Margin="0,0,5,0">
            <dxdo:LayoutControlItem ShowCaption="False">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Width="230">
                    <ItemsControl Name="ctlSelect" MinHeight="40">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <dx:SimpleButton Margin="5" Tag="{Binding}" x:Name="btnSelected" Click="BtnSelected_Click">
                                    <Grid MinWidth="76" Height="40">
                                        <Label Content="{Binding Defect.Description}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        <TextBlock VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0 -4 -1 0" Text="x"/>
                                        <TextBlock Visibility="{Binding Control.AllowQty,Converter={StaticResource QtyVisibilityConverter}}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0 0 -1 0" Text="{Binding Qty}"/>
                                    </Grid>
                                </dx:SimpleButton>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True"></WrapPanel>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </dxdo:LayoutControlItem>
        </dxdo:LayoutGroup>

        <dxdo:LayoutGroup Caption="{Binding [缺陷分类],Source={uic:L10N}}" DockPanel.Dock="Top" ShowCaption="True" GroupBorderStyle="GroupBox" ItemHeight="Auto"  Margin="0,0,0,5">
            <dxdo:LayoutControlItem ShowCaption="False">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <dx:SimpleButton Margin="5 5 0 5" MinWidth="60" Height="30" x:Name="btnAll" Click="BtnAll_Click" Content="{Binding [所有],Source={uic:L10N}}"/>
                        <ItemsControl Name="ctlNavigation">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <dx:SimpleButton Margin="2 5 0 5" MinWidth="60" Height="30" x:Name="btnNavigation" Click="BtnNavigation_Click" Content="{Binding Category.Description}" Tag="{Binding}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                    <ItemsControl Name="ctlCategory">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <dx:SimpleButton  Margin="5" MinWidth="80" Height="40" x:Name="btnCategory" Click="BtnCategory_Click" Content="{Binding Category.Description}" Tag="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </StackPanel>
            </dxdo:LayoutControlItem>
        </dxdo:LayoutGroup>

        <dxdo:LayoutGroup Caption="{Binding [缺陷代码],Source={uic:L10N}}" DockPanel.Dock="Top" ShowCaption="True" GroupBorderStyle="GroupBox" AllowExpand="False" ItemHeight="Auto">
            <dxdo:LayoutControlItem ShowCaption="False">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <ItemsControl Name="ctlDefect">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <dx:SimpleButton Margin="5" Width="80" Height="40" x:Name="btnDefect" Click="BtnDefect_Click" Content="{Binding Defect.Description}" Tag="{Binding}"></dx:SimpleButton>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
            </dxdo:LayoutControlItem>
        </dxdo:LayoutGroup>
    </DockPanel>
</UserControl>
